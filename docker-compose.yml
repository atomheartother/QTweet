version: "3.7"

services:
  qtweet:
    build: .
    image: navenn_t/qtweet
    volumes:
      - qtweetData:/usr/src/app/data/
    environment:
      - TZ=Europe/Paris

volumes:
  qtweetData:
    external: true